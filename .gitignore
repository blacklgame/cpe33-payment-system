node_modules/
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

*.zip

# Roster PII (names/emails/Nu IDs) -- used only by scripts/seed-users.js,
# never deployed. Keep your own local copy; don't commit it.
scripts/roster-data.json
serviceAccountKey.json

