# Copy to .env.local for local dev (gitignored). In Vercel, set the
# same var under Project -> Settings -> Environment Variables.

# Comma-separated list of admin emails (replaces config/admin-emails.json)
ADMIN_EMAILS=admin1@example.ac.th,admin2@example.ac.th

FIREBASE_SERVICE_ACCOUNT_BASE64=
