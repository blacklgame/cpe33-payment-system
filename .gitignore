node_modules/
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

*.zip

# Roster PII (names/emails/Nu IDs) -- used only by scripts/seed-users.js,
# never deployed. Keep your own local copy; don't commit it.
scripts/roster-data.json
serviceAccountKey.json

# Real admin emails -- set ADMIN_EMAILS in Vercel's env vars instead
# (see api/_lib/admins.js). This file is now only a local-dev fallback
# if you don't want to set the env var on your own machine; don't put
# real @nu.ac.th admin addresses in a committed file. Commit
# config/admin-emails.example.json (placeholder values) instead.
config/admin-emails.json

